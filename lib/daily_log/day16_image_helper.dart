// [Day 16] 이미지 선택 헬퍼
// 파일명: utils/image_helper.dart

// (가상의 코드: image_picker 패키지 필요)
// import 'package:image_picker/image_picker.dart';
// import 'dart:io';

class ImageHelper {
  // 1. [키워드: Picker] 앨범에서 사진을 가져오는 도구 생성
  // final ImagePicker _picker = ImagePicker();

  Future<dynamic> pickImage() async {
    // 2. [동작: Get Image] 갤러리 실행 및 파일 선택 대기
    // final XFile? image = await _picker.pickImage(source: ImageSource.gallery);
    
    // if (image != null) {
    //   return File(image.path); // 3. 선택된 파일을 반환
    // }
    return null;
  }
}